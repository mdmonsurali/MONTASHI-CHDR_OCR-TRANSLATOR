fastapi==0.136.3
uvicorn[standard]==0.37.0
python-multipart==0.0.20

httpx==0.27.2
docker==7.1.0

# Direct artifact serving: read from MinIO + postgres so users can download
# translated DOCX / JSON / preview while translator_service is stopped
# between batches.
asyncpg==0.31.0
minio==7.2.20
