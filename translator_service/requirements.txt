fastapi==0.136.3
uvicorn[standard]==0.37.0
python-multipart==0.0.20

httpx==0.27.2

pillow==11.3.0
python-docx==1.2.0
lxml==5.3.0

# Formula rendering inside document_reconstruction.json_to_docx — same
# version as ocr_service to keep the bundled copy of the module in sync.
matplotlib==3.10.9

asyncpg==0.31.0
minio==7.2.20
